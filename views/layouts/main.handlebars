<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between m-5 align-center bg-secondary-subtle" data-bs-theme="dark">
      <h1>
        <a class="text-dark bg-white shadow p-3 m-5 rounded" href="/">The Tech Blog</a>
      </h1>
      <nav class="nav justify-space-between m-3 fs-lg">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="nav-link btn" href="/">home</a>
        <a class="nav-lin btn" href="/dashboard">dashboard</a>
        <a class="nav-link btn" id="logout">logout</a>
        {{else}}
        <a class="nav-link btn" href="/login">login</a>
        {{/if}}
      </nav>
     
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>

</html>
